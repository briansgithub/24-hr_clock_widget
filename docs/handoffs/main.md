# Branch: `main`

## Identity

- Tip: `2eaab30` — Adjust bathyphase and acrophase indicator sizing and z-order
- Upstream: `origin/main` at `3c5a7bd`; local is 1 commit ahead
- Role: shared feature-PR base after publication
- Last verified: 2026-07-17

## Goal and scope

Maintain the stable integration branch. Current unpublished scope is limited to Python bathyphase/acrophase indicator sizing and z-order.

## Changes since current remote base

- `2eaab30`: adjusted indicator sizing/z-order in `python/clock_widget.py` and `python/energy_logic.py`.

## Current status

- `2eaab30` is committed locally but not published to `origin/main`.
- Current feature working-tree edits belong to `feature/empirical-public-priority`, not `main`.
- No validation results are recorded for publishing `2eaab30`.

## Next actions

1. Confirm whether any checks are desired for the already-committed indicator change.
2. Publish `2eaab30` to `origin/main`.
3. Use updated `origin/main` as the base for the dynamic-sun-color checkpoint PR.

## Merge and cleanup

- Readiness: ready to publish after owner-confirmed checks, if any.
- Required order: publish before either planned feature PR.
- Keep local `main` aligned with `origin/main` after each merge.

